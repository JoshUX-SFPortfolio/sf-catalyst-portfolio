# Workflow 2 — Develop Integration
# Trigger: push to the develop branch (i.e. after a feature branch is merged via PR)
# Purpose: confirm the integrated state of develop is always deployable
#
# This is the second line of defence after Workflow 1 (PR Validation).
# Workflow 1 validates the incoming branch; Workflow 2 validates the merged result.
# If this fails, develop contains a bad integration — the team is notified to fix before
# any further merges land.
#
# What this does:
#   1. Authenticates to Salesforce using JWT-based auth (no passwords stored in GitHub)
#   2. Creates a fresh scratch org from project-scratch-def.json
#   3. Deploys the current develop metadata to the scratch org
#   4. Runs all Apex tests with code coverage reporting
#   5. Fails if any test fails or coverage < 85%
#   6. Always deletes the scratch org
#
# Required GitHub Secrets (Settings → Secrets → Actions):
#   SF_CLIENT_ID     — Connected App consumer key
#   SF_USERNAME      — Salesforce username for the CI user
#   SF_SERVER_KEY    — Base64-encoded JWT private key (base64 -i server.key)
#   SF_INSTANCE_URL  — Target org login URL

name: Develop Integration

on:
  push:
    branches: [develop]

jobs:
  integrate:
    name: Integration — Deploy & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install SF CLI
        run: npm install --global @salesforce/cli@latest

      - name: JWT authenticate to Salesforce
        run: |
          echo "${{ secrets.SF_SERVER_KEY }}" | base64 -d > server.key
          sf org login jwt \
            --client-id "${{ secrets.SF_CLIENT_ID }}" \
            --jwt-key-file server.key \
            --username "${{ secrets.SF_USERNAME }}" \
            --instance-url "${{ secrets.SF_INSTANCE_URL }}" \
            --alias ci-org --set-default-dev-hub
          rm -f server.key

      - name: Create scratch org
        run: |
          sf org create scratch \
            --definition-file config/project-scratch-def.json \
            --alias ci-develop-scratch \
            --duration-days 1 \
            --wait 15 \
            --target-dev-hub ci-org

      - name: Deploy metadata
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org ci-develop-scratch \
            --wait 20

      - name: Run Apex tests with coverage
        run: |
          sf apex run test \
            --target-org ci-develop-scratch \
            --code-coverage \
            --result-format json \
            --wait 15 \
            --output-dir test-results \
            > test-results/test-output.json || true

      - name: Assert coverage >= 85%
        run: |
          COVERAGE=$(jq -r '.result.summary.orgWideCoverage // "0%"' test-results/test-output.json | tr -d '%')
          PASS_COUNT=$(jq -r '.result.summary.passing // 0' test-results/test-output.json)
          FAIL_COUNT=$(jq -r '.result.summary.failing // 0' test-results/test-output.json)
          echo "Apex tests: ${PASS_COUNT} passed, ${FAIL_COUNT} failed"
          echo "Org-wide coverage: ${COVERAGE}%"
          if [ "$FAIL_COUNT" -gt 0 ]; then
            echo "FAIL: ${FAIL_COUNT} Apex test(s) failed."
            exit 1
          fi
          if [ "$(echo "$COVERAGE < 85" | bc -l)" -eq 1 ]; then
            echo "FAIL: Coverage ${COVERAGE}% is below the 85% threshold."
            exit 1
          fi
          echo "PASS: All tests passing, coverage ${COVERAGE}% meets the 85% threshold."

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: apex-test-results-develop
          path: test-results/

      - name: Delete scratch org
        if: always()
        run: sf org delete scratch --target-org ci-develop-scratch --no-prompt
